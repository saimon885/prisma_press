enum Status {
    ACTIVE
    INACTIVE
}

enum Role {
    USER
    AUTHOR
    ADMIN
}

enum Cstatus {
    APPROVED
    REJECT
}

enum Featured {
    true
    false
}

enum postStatus {
    Draft
    published
    Archived
}
